interface:
  display_name: "Replicate Web UI"
  short_description: "Rebuild web visuals, interactions, and motion"
  default_prompt: "Use $replicate-web-ui to analyze this URL, screenshot, or video and rebuild the experience in my existing frontend project."
